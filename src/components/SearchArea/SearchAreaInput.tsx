import React, { FC } from 'react'

const SearchAreaInput: FC<{
  label: string
  content: React.ReactNode
  description: string
}> = ({ label, content, description }) => {
  return (
    <div>SearchAreaInput</div>
  )
}

export default SearchAreaInput